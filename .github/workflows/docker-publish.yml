name: docker

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      ref:
        description: "Git tag to build and publish (e.g. v0.1.1)"
        required: true
        default: "v0.1.1"

permissions:
  contents: read
  packages: write # push the image
  id-token: write # keyless cosign signing via GitHub OIDC
  attestations: write # build-provenance attestation

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.ref || github.ref }}
      - id: ver
        # Tag name without the leading v (v0.1.1 -> 0.1.1) for the image tag and
        # the stamped build version. Move :latest only for a real release: a
        # prerelease tag (e.g. v0.2.0-rc1, which has a hyphen) must not.
        run: |
          REF="${{ github.event.inputs.ref || github.ref_name }}"
          VER="${REF#v}"
          echo "version=$VER" >> "$GITHUB_OUTPUT"
          TAGS="ghcr.io/${{ github.repository }}:$VER"
          case "$VER" in
            *-*) ;;                                                   # prerelease: no :latest
            *) TAGS="$TAGS,ghcr.io/${{ github.repository }}:latest" ;;
          esac
          echo "tags=$TAGS" >> "$GITHUB_OUTPUT"
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.ver.outputs.tags }}
          build-args: |
            VERSION=${{ steps.ver.outputs.version }}
      # Keyless cosign signature over the image manifest digest (covers every tag
      # pointing at it), plus a SLSA build-provenance attestation pushed to GHCR.
      - uses: sigstore/cosign-installer@v3
      - name: Sign the image
        run: cosign sign --yes "ghcr.io/${{ github.repository }}@${{ steps.build.outputs.digest }}"
      - name: Attest image provenance
        uses: actions/attest-build-provenance@v2
        with:
          subject-name: ghcr.io/${{ github.repository }}
          subject-digest: ${{ steps.build.outputs.digest }}
          push-to-registry: true
